# Requirements:

# Study the code on what it does and then modify it to meet the requirements below.
# When a user enters a year, it should call  isLeapYear() function and pass the year entered by the user.
# Inside isLeapYear() function determine if the entered year is a leap year or not and 
#   returns a boolean value true or false respectively.
# Based on the returned value of isLeapYear() function, 
#   display a message in the format of either yyyy is a leap year or yyyy is not a leap year. 
#   Wherein yyyy is the entered year by the user.
# A leap year is exactly divisible by 4 except for century years (years ending with 00). 
#   The century year is a leap year only if it is perfectly divisible by 400.

# Code template to use with function call to isLeapYear(year)
def isLeapYear(year):
     return result


# Asking the user for input
year = int(input("Enter a year: "))
# call the function isLeapYear
